# SPDX-License-Identifier: GPL-2.0-only

load("@kleaf//build/kernel/kleaf:kernel.bzl", "checkpatch", "ddk_module")

package(default_visibility = [
    "//private/devices/google:__subpackages__",
    "//private/google-modules:__subpackages__",
])

checkpatch(
    name = "checkpatch",
    checkpatch_pl = "//common:scripts/checkpatch.pl",
)

filegroup(
    name = "sscoredump.kconfig",
    srcs = [
        "Kconfig",
    ],
    visibility = [
        "//private/devices/google:__subpackages__",
    ],
)

ddk_module(
    name = "sscoredump",
    srcs = [
        "sscoredump.c",
        "sscoredump.h",
    ],
    out = "sscoredump.ko",
    kconfig = "Kconfig",
    kernel_build = "//private/devices/google/common:kernel",
    deps = [
        "//common:all_headers_aarch64",
        "//private/google-modules/soc/gs:headers",
    ],
)

ddk_module(
    name = "sscoredump_kunit_test",
    srcs = [
        "sscoredump.h",
        "test/sscoredump_test.c",
    ],
    out = "sscoredump_kunit_test.ko",
    defconfig = "test/kunit_defconfig",
    kconfig = "test/Kconfig",
    kernel_build = "//private/devices/google/common:kernel",
    deps = [
        ":sscoredump",
        "//common:all_headers_aarch64",
        "//private/google-modules/soc/gs:headers",
    ],
)
